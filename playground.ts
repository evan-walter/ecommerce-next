

export default function play() {
// Two different means to define arrays
const names: string[] = ['Deer', 'John']
const numbers: Array<number> = [1, 2, 3, 4, 5]

  const random = Math.random() > 0.5 ? 'hello' : [1, 2]
}
